// Archivo proxy.ts generado para reemplazar middleware.ts según la nueva convención de Next.js
// Puedes migrar la lógica de middleware aquí si es necesario.

export { default } from "./middleware";
